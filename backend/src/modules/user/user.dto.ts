export type UserDTO = {
  id?: string;
  name: string;
  email: string;
  password: string;
  passwordRec: string;
  status: string;
};

// Usado para definir a estrutuda de dados que será usada para criar um novo usuário
